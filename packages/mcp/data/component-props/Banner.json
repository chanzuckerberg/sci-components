{
  "Banner": {
    "props": {
      "children": {
        "description": "Required. The content of the Banner, laid out beside the icon and centered in the space left by the close button.",
        "isRequired": true,
        "type": "ReactNode"
      },
      "dismissed": {
        "description": "If true, the banner has been dismissed and renders nothing. Leave this unset to let the Banner keep track of its own dismissed state. Passing any value, false included, takes that over: the close button then only fires onClose, and the Banner closes when you set dismissed to true.",
        "isRequired": false,
        "type": "boolean"
      },
      "dismissible": {
        "defaultValue": true,
        "description": "If true, the banner can be dismissed by the user. This is what controls whether the close button is rendered.",
        "isRequired": false,
        "type": "boolean"
      },
      "icon": {
        "description": "The name of an SDS icon, or a custom SVG element, shown in place of the icon the intent would pick. Without it, \"positive\" shows \"CheckCircle\", \"negative\" and \"notice\" show \"ExclamationMarkCircle\", and every other intent shows \"InfoCircle\".",
        "isRequired": false,
        "type": "ReactElement"
      },
      "intent": {
        "defaultValue": "info",
        "description": "The intent color of the Banner component, which also selects the default icon.",
        "isRequired": false,
        "type": "\"info\" | \"accent\" | \"negative\" | \"notice\" | \"positive\""
      },
      "onClose": {
        "description": "Callback fired when the user clicks the close button. Signature: function(event: React.MouseEvent) => void event The event source of the callback.",
        "isRequired": false,
        "type": "function"
      },
      "sdsIconProps": {
        "description": "Additional props to be passed to the icon component.",
        "isRequired": false,
        "type": "IconProps<string | number | symbol>"
      },
      "sdsType": {
        "description": "Required. The primary variant fills the Banner with the intent's primary surface color and uses the on-fill text color. The secondary variant uses the intent's secondary surface color with the standard primary text color.",
        "isRequired": true,
        "type": "\"primary\" | \"secondary\""
      }
    }
  }
}
