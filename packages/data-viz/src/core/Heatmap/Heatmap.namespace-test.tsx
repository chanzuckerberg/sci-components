import { Heatmap, HeatmapProps } from "@czi-sds/data-viz";
import React from "react";

const HeatmapNameSpaceTest = (props: HeatmapProps) => {
  return (
    <Heatmap>
      <p>Heatmap!</p>
    </Heatmap>
  );
};
