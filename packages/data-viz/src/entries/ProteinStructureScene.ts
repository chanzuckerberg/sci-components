/**
 * Subpath entry: `@czi-sds/data-viz/ProteinStructureScene`.
 *
 * The structure scene on its own, for drawing on a Mol* plugin or rendering an
 * image without the viewer - and without the React it is built on.
 */

export * from "../core/ProteinStructureViewer/scene";
