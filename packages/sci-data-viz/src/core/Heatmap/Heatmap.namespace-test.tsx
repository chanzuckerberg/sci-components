import { Heatmap, HeatmapProps } from "@czifui/sci-data-viz";
import React from "react";

const HeatmapNameSpaceTest = (props: HeatmapProps) => {
  return (
    <Heatmap>
      <p>Heatmap!</p>
    </Heatmap>
  );
};
